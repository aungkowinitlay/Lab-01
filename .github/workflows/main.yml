name: Self-Hosted Runner Test

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Say hello from VM
        run: echo "✅ Hello from the VirtualBox self-hosted runner!"
